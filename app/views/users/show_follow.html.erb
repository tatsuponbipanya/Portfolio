<% provide(:title, @title) %>
<div class="row">
  <div class="col-md-4">
    <section class="user_info">
      <h3>プロフィール</h3>
      <%= gravatar_for @user %>
      <%= @user.name %><br>
      <span><b>記事投稿数:</b> <%= @user.microposts.count %></span><br>
      <span><%= link_to "プロフィール", @user %></span>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.each do |user| %>
            <%= link_to gravatar_for(user, size: 30), user %>
          <% end %>
        </div>
      <% end %>
    </section>
  </div>
  <div class="col-md-8">
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>
